{
  "name": "shawns-personal-fitness",
  "version": "1.0.0",
  "private": true,
  "description": "business-website",
  "author": "lokisk1155",
  "keywords": [
    "gatsby"
  ],
  "scripts": {
    "deploy": "git config user.email 'github-actions@example.com' && git config user.name 'GitHub Actions' && gatsby build --prefix-paths && gh-pages -d public",
    "develop": "gatsby develop",
    "start": "gatsby develop",
    "build": "gatsby build",
    "serve": "gatsby serve",
    "clean": "gatsby clean",
    "typecheck": "tsc --noEmit"
  },
  "dependencies": {
    "gatsby": "^5.8.1",
    "gh-pages": "^5.0.0",
    "react": "^18.2.0",
    "react-dom": "^18.2.0"
  },
  "devDependencies": {
    "@types/node": "^18.15.11",
    "@types/react": "^18.0.31",
    "@types/react-dom": "^18.0.11",
    "prettier": "^2.8.7",
    "typescript": "^5.0.3"
  }
}
